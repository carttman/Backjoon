#include <iostream>
#include <map>
#include <vector>

using namespace std;

int main()
{
	// 한 번 입은 옷 조합은 다시 입지않는다.
	// 가진 의상의 수가 주어진다.
	// 가진 의상의 이름과 종류가 주어진다. 같은 종류의 의상은 하나만 입을 수 있다.

	int T, N;

	cin >> T;
	cin >> N;

	vector<multimap<string, string>> A;

	for (int i=0; i<T; i++)
	{
		//for (int j=0; i<)
	}

}